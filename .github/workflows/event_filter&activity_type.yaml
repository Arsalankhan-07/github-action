name: Event Filters and Acitivity Types

on:
    # pull_request:
    #     types: [opened]
    #     branches:
    #         - main
    workflow_dispatch:

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Running whenever a PR is opened and the base branches is main"